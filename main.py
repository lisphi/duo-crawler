import duolingo


def main():
    crawler = duolingo.DuoCrawler()
    # crawler.download_courses(False)
    # crawler.download_story_static_files(False)
    # crawler.generate_story_mp3_files(False)
    # crawler.download_part_static_files(False)
    # crawler.generate_part_mp3_files(False)

if __name__ == '__main__':
    main()